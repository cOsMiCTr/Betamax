

import { area, circumference, onurTest } from "./circle.js";
let r = 3;
let x = 10;
console.log(
  `Circle with radius ${r} has area: ${area(r)};
  circumference: ${circumference(r)}
  and this is Onurs Test result: ${onurTest(r,x)}`);
